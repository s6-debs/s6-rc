Source: s6-rc
Section: admin
Priority: optional
Maintainer: multi <multi@in-addr.xyz>
Build-Depends:
 debhelper (>= 10),
 libskarnet-dev (>= 2.10.0.3),
 libexecline-dev (>= 2.8.0.1),
 libs6-dev (>= 2.10.0.3),
 quilt
Standards-Version: 3.9.8
Homepage: https://skarnet.org/software/s6-rc/

Package: s6-rc
Architecture: any
Section: admin
Depends:
 ${misc:Depends},
 ${shlibs:Depends},
 execline (>= 2.8.0.1),
 s6 (>= 2.10.0.3)
Description: service manager for skarnet's s6 process supervisor
 s6-rc is a service manager for s6-based systems, i.e. a suite of programs
 that can start and stop services, both long-running daemons and one-time
 initialization scripts, in the proper order according to a dependency tree.
 It ensures that long-running daemons are supervised by the s6 infrastructure,
 and that one-time scripts are also run in a controlled environment.

Package: libs6rc0.5
Architecture: any
Multi-Arch: same
Section: libs
Depends: ${misc:Depends}, ${shlibs:Depends}
Description: service manager for skarnet's s6 process supervisor (runtime libraries)
 s6-rc is a service manager for s6-based systems, i.e. a suite of programs
 that can start and stop services, both long-running daemons and one-time
 initialization scripts, in the proper order according to a dependency tree.
 It ensures that long-running daemons are supervised by the s6 infrastructure,
 and that one-time scripts are also run in a controlled environment.
 .
 This package contains the s6-rc runtime libraries.

Package: libs6rc-dev
Architecture: any
Multi-Arch: same
Section: libdevel
Depends:
 ${misc:Depends},
 libs6rc0.5 (= ${binary:Version}),
 libskarnet-dev (>= 2.10.0.3),
 libexecline-dev (>= 2.8.0.1),
 libs6-dev (>= 2.10.0.3)
Description: service manager for skarnet's s6 process supervisor (development files)
 s6-rc is a service manager for s6-based systems, i.e. a suite of programs
 that can start and stop services, both long-running daemons and one-time
 initialization scripts, in the proper order according to a dependency tree.
 It ensures that long-running daemons are supervised by the s6 infrastructure,
 and that one-time scripts are also run in a controlled environment.
 .
 This package contains the s6-rc development files.
